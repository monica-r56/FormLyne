import { defineMessages } from 'react-intl';

const messages = defineMessages({
  thumbLabel: {
    id: 'molecules.timeline.thumbLabel',
    defaultMessage: 'Thumb',
  },
});

export default messages;
